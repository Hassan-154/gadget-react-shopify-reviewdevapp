/**
* Validation code for field ratingNumber on reviewList
* @param {import("gadget-server").ReviewListRatingNumberFieldValidationContext } context - All the useful bits for running this validation.
*/
export default async ({api, record, errors, logger, field}) => {
  // TODO: implement validation
  // access things like `record.ratingNumber` to get the fields's data
  // add to the `errors` with `errors.add('ratingNumber', "did not pass the validation")`
};